#include <cstdio>
#include <cstring>
#include <cstdarg>
#include <algorithm>
#include <list>
#include <queue>
#include <vector>
#include <cmath>
using namespace std;
typedef long long
void gauss_jordan(int n){
  for(int i = 0; i < n; i++){
    int r = i;
    for(j = i+1; j < n; j++)if(fabs(A[j][i]) > fabs(A[r][i]))r = j;
    if(!A)
  }
}
int main(){
  
  return 0;
}